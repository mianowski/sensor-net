
ADD_DEFINITIONS( ${RCF_DEFINES} )

INCLUDE_DIRECTORIES(
    ${RCF_INCLUDES} 
    ${CMAKE_SOURCE_DIR}/include
)

ADD_EXECUTABLE(
    Sensor
    Sensor.cpp)

TARGET_LINK_LIBRARIES( Sensor RcfLib ${RCF_LIBS} )
